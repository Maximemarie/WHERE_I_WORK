<div class = "background">
 <div class = "cardreview">
  <h3>New review</h3>

  <%= simple_form_for([@place, @review]) do |f| %>
    <div class= "comment" >
    <%= f.input :content %>
    <%= f.input :rating, collection: 1..5, as: :integer %>

      <%= f.submit class:"button"%>

  <% end %>
  </div>
  </div>
</div>
